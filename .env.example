# Supabase configuration (optional — for database-backed view counts)
# 1. Create a project at https://supabase.com
# 2. Go to Project Settings → API Keys
# 3. Copy your Project URL and publishable key
# 4. Create a .env file (local dev) or set GitHub repo secrets (CI/CD)
#
# Note: The publishable key (sb_publishable_*) is safe to expose in the
# frontend — security is enforced by Row Level Security (RLS) policies.
# NEVER expose the secret key (sb_secret_*) in frontend code.
#
# The Data REST API endpoint is auto-generated at:
#   https://<project_ref>.supabase.co/rest/v1/
# No separate env var needed — the Supabase client handles it.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Twikoo comment system (optional — for blog post & note comments)
# 1. Deploy Twikoo backend: https://twikoo.js.org/backend.html
#    (Vercel, CloudBase, or self-hosted — Vercel is recommended)
# 2. After deployment, copy the backend URL (e.g. https://your-twikoo.vercel.app)
# 3. Set it as VITE_TWIKOO_ENV_ID below
# 4. Create a .env file (local dev) or set GitHub repo secrets (CI/CD)
#
# Note: The env ID is the Twikoo backend URL — it is safe to expose in the
# frontend. Access control is managed in the Twikoo admin panel.
# If left empty, the comment section will not be rendered.

VITE_TWIKOO_ENV_ID=
